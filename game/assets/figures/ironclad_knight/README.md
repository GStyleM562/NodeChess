# Ironclad Knight (Meshy name: "Iron Bastion")

GDD sheet: `docs/Part 2/GDD_v1.0_Part2A_StarterRoster_MVP.md` → §1.

Drop here: **one merged `ironclad_knight.glb`** — model + textures + all Tier 1 clips on one rig
(Meshy "put everything in one file"; Origin: Bottom, GLB, textures embedded).

Tier 1 clips: `idle, move_walk, attack, attack_heavy, defend, hit, ko` (+ `move_run`/`deploy` optional).
Humanoid → Mixamo can fill any clip Meshy lacks. See the golem (`stone_golem/stone_golem.glb`) as the
reference for a correct export.

See `game/assets/ASSETS_GUIDE.md` for the full workflow.
