# Ironclad Knight (Meshy name: "Iron Bastion")

GDD sheet: `docs/Part 2/GDD_v1.0_Part2A_StarterRoster_MVP.md` → §1.

Drop here:
- `ironclad_knight.glb` — base rigged model (Origin: Bottom, GLB, textures embedded).
- `animations/ironclad_knight_<clip>.glb` — one per clip (or a single GLB with all clips).

Tier 1 clips: `idle, move_walk, attack, attack_heavy, defend, hit, ko` (+ `deploy` optional).
Humanoid → Mixamo can fill any clip Meshy lacks.

See `game/assets/ASSETS_GUIDE.md` for the full workflow.
